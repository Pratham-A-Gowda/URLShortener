name: React App CI (Docker Build Disabled)

on:
  push:
    branches:
      - "**"       # Run on all branches
  pull_request:
    branches:
      - "**"       # Run on all PRs
  workflow_dispatch:

jobs:
  skip-build:
    runs-on: ubuntu-latest

    steps:
      - name: Note
        run: echo "Docker build disabled. Remove this file or add steps if needed."
